# Streamlit Cloud 部署指南

## 📋 准备工作

### 1. 需要的文件（已准备好）
- ✅ `streamlit_app.py` - 主程序
- ✅ `ai_generator.py` - AI生成器
- ✅ `requirements.txt` - 依赖列表
- ✅ `.gitignore` - Git忽略文件
- ✅ `.streamlit/config.toml` - 配置文件

### 2. 需要的账号
- GitHub账号（用于存放代码）
- Streamlit Cloud账号（免费，用GitHub登录）

## 🚀 部署步骤

### 步骤1：创建GitHub仓库

1. **访问GitHub**：https://github.com
2. **创建新仓库**：
   - 点击右上角 "+" → "New repository"
   - 仓库名称：`ui-test-case-generator`（或其他名称）
   - 设置为 Public（公开）
   - 不要勾选 "Initialize with README"
   - 点击 "Create repository"

### 步骤2：上传代码到GitHub

在当前目录执行以下命令：

```bash
# 初始化Git仓库
git init

# 添加所有文件
git add streamlit_app.py ai_generator.py requirements.txt .gitignore .streamlit/

# 提交
git commit -m "Initial commit: UI走查用例生成助手"

# 关联远程仓库（替换为你的GitHub用户名和仓库名）
git remote add origin https://github.com/你的用户名/ui-test-case-generator.git

# 推送到GitHub
git branch -M main
git push -u origin main
```

**如果遇到权限问题**：
- 使用GitHub Personal Access Token
- 或使用GitHub Desktop客户端

### 步骤3：部署到Streamlit Cloud

1. **访问Streamlit Cloud**：https://streamlit.io/cloud

2. **登录**：
   - 点击 "Sign in"
   - 选择 "Continue with GitHub"
   - 授权Streamlit访问你的GitHub

3. **创建新应用**：
   - 点击 "New app"
   - 选择你的仓库：`你的用户名/ui-test-case-generator`
   - Branch: `main`
   - Main file path: `streamlit_app.py`
   - 点击 "Deploy!"

4. **等待部署**：
   - 首次部署需要3-5分钟
   - 可以看到实时日志
   - 部署成功后会自动打开应用

### 步骤4：配置Secrets（可选）

如果要预配置API Key：

1. 在Streamlit Cloud应用页面
2. 点击 "Settings" → "Secrets"
3. 添加配置：
```toml
DEEPSEEK_API_KEY = "your-api-key-here"
```

然后在代码中读取：
```python
import streamlit as st
api_key = st.secrets.get("DEEPSEEK_API_KEY", "")
```

## 🌐 访问应用

部署成功后，你会得到一个公网地址：
```
https://你的用户名-ui-test-case-generator-xxx.streamlit.app
```

这个地址可以分享给任何人访问！

## 📝 更新应用

当你修改代码后：

```bash
# 提交更改
git add .
git commit -m "更新说明"
git push

# Streamlit Cloud会自动检测并重新部署
```

## ⚠️ 注意事项

### 1. 文件大小限制
- 上传文件最大10MB
- 如需更大，修改 `.streamlit/config.toml` 中的 `maxUploadSize`

### 2. 资源限制
- 免费版有资源限制
- 如果应用长时间无人访问会休眠
- 首次访问可能需要几秒钟唤醒

### 3. 隐私保护
- 不要在代码中硬编码API Key
- 使用Streamlit Secrets管理敏感信息
- 用户上传的文件不会永久保存

### 4. 依赖管理
- 确保 `requirements.txt` 包含所有依赖
- 版本号要明确，避免兼容性问题

## 🔧 故障排查

### 部署失败
1. 检查 `requirements.txt` 是否正确
2. 查看部署日志中的错误信息
3. 确保所有依赖都能正常安装

### 应用运行错误
1. 在本地先测试是否正常运行
2. 检查是否有文件路径问题
3. 查看Streamlit Cloud的日志

### 无法访问
1. 检查应用是否在运行状态
2. 尝试重启应用
3. 检查网络连接

## 💡 优化建议

### 1. 添加缓存
```python
@st.cache_data
def load_data():
    # 缓存数据加载
    pass
```

### 2. 优化性能
- 减少不必要的重新运行
- 使用 `st.spinner()` 显示加载状态
- 压缩输出文件

### 3. 改进用户体验
- 添加使用说明
- 提供示例文档
- 添加错误提示

## 📞 获取帮助

- Streamlit文档：https://docs.streamlit.io
- Streamlit社区：https://discuss.streamlit.io
- GitHub Issues：在你的仓库中创建Issue

## 🎉 完成！

现在你的应用已经部署到云端，任何人都可以通过公网地址访问了！
