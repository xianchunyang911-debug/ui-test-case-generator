# {功能名称}UI走查计划

> **使用说明**：本文档为UI走查计划模板，使用时请将 `{功能名称}` 等占位符替换为实际内容。基于需求文档和流程图自动生成时，系统会自动填充相关内容。

## 1. 走查目标

### 1.1 总体目标
确保{功能名称}功能的UI实现与设计稿在视觉和体验上保持一致，验证所有UI元素、交互状态和异常流程的完整性和正确性。

### 1.2 具体目标
- **视觉一致性**：验证颜色、字体、图标、间距等视觉元素符合设计规范
- **功能完整性**：确保所有功能模块的UI元素完整，无遗漏
- **交互流畅性**：验证用户操作流程的UI反馈及时、准确
- **异常处理**：确保异常场景下的UI表现优雅、友好
- **响应式适配**：验证不同屏幕尺寸下的布局表现
- **可访问性**：确保符合可访问性标准，支持键盘导航和屏幕阅读器

### 1.3 验收标准
- 所有UI元素与设计稿一致度达到100%
- 所有交互状态（默认、悬停、点击、禁用、加载等）都有对应的UI表现
- 所有异常场景都有友好的错误提示和处理机制
- 所有页面在不同屏幕尺寸下都能正常显示

---

## 2. 走查方法

### 2.1 走查方式说明

UI走查采用**两种方式相结合**的方法，确保既覆盖所有UI元素，又验证整体用户体验的连贯性：

#### 方式一：按点走查（用例走查）
- **定义**：按照UI走查用例清单，逐条检查每个UI元素和检查点
- **目的**：确保所有UI元素都被检查到位，无遗漏
- **适用场景**：
  - 首次走查，确保全面覆盖
  - 修复验证，确保问题已解决
  - 回归走查，确保无新问题
- **执行方式**：按照CSV用例清单，逐条执行并记录结果

#### 方式二：按流程走查（场景走查）
- **定义**：模拟真实用户操作流程，按照业务流程进行端到端走查
- **目的**：验证整体用户体验的连贯性、流畅性和逻辑性
- **适用场景**：
  - 核心业务流程验证
  - 跨页面交互验证
  - 用户体验流畅性验证
- **执行方式**：按照业务流程流程图，完整执行用户操作路径，检查每个环节的UI表现

### 2.2 两种方式的结合使用

**推荐走查顺序**：
1. **先按流程走查**：快速验证核心流程的完整性和流畅性，发现明显的体验问题
2. **再按点走查**：系统性地检查每个UI元素，确保无遗漏
3. **最后流程验证**：修复问题后，再次按流程走查，验证整体体验

**两种方式的互补性**：
- 按点走查：发现细节问题（颜色、字号、间距、状态等）
- 按流程走查：发现体验问题（跳转逻辑、状态同步、数据传递等）

### 2.3 核心业务流程清单

基于需求文档和流程图，识别以下核心业务流程需要按流程走查：

> **自动生成说明**：系统会根据需求文档和流程图自动识别核心业务流程，并生成流程清单表格。格式如下：

| 流程编号 | 流程名称 | 流程路径 | 检查重点 |
|----------|----------|----------|----------|
| 流程1 | {流程名称1} | {页面1} → {页面2} → {页面3} | {检查重点1} |
| 流程2 | {流程名称2} | {页面A} → {页面B} | {检查重点2} |
| 流程N | {流程名称N} | {流程路径N} | {检查重点N} |

**流程识别规则**：
- 从需求文档中提取主要功能模块和操作流程
- 从流程图中识别关键路径和决策点
- 识别正常流程、异常流程、边界流程

### 2.4 流程走查检查要点

在执行流程走查时，重点关注以下方面：

- **页面跳转**：跳转是否流畅，是否有过渡动画，数据是否正确传递
- **状态同步**：跨页面的状态是否同步（如主任务状态与子任务状态）
- **数据传递**：页面间数据传递是否正确，返回时状态是否保持
- **操作反馈**：每个操作是否有及时、明确的反馈
- **错误处理**：异常情况下的UI表现是否友好
- **视觉连贯性**：整个流程中的视觉风格是否统一

---

## 3. 走查团队信息

| 角色 | 姓名 | 联系方式 | 职责 |
|------|------|----------|------|
| UI设计师 | 【待填写】 | 【待填写】 | 提供设计稿、设计规范，参与走查评审 |
| 前端开发 | 【待填写】 | 【待填写】 | 实现UI界面，修复UI问题 |
| 后端开发 | 【待填写】 | 【待填写】 | 提供接口支持，配合前端调试 |
| UI走查负责人 | 【待填写】 | 【待填写】 | 组织走查活动，记录问题，跟踪修复 |
| 产品经理 | 【待填写】 | 【待填写】 | 确认需求理解，参与验收 |

**备注**：请填写实际团队成员信息

---

## 4. 走查时间计划

**走查开始日期**：【待填写】

**预计完成日期**：【待填写】

**总计预计耗时**：【待填写】天

> **估算建议**：根据模块数量和复杂度，一般每个模块0.5-1天，全局检查0.5天，问题修复验证1-2天。

---

## 5. 走查模块清单

### 5.1 模块统计
本次走查共涉及 **{模块数量}个主要模块**，**{用例数量}个UI走查用例**。

> **自动生成说明**：系统会根据需求文档自动识别功能模块，统计模块数量和用例数量。

### 5.2 模块详细清单

> **自动生成说明**：系统会根据需求文档的章节结构或设计稿的页面结构，自动生成模块清单表格。格式如下：

| 序号 | 模块名称 | 页面/功能 | 用例数量 | 优先级 | 备注 |
|------|----------|----------|----------|--------|------|
| 1 | {模块名称1} | {页面/功能描述1} | {用例数量1} | {高/中/低} | {备注1} |
| 2 | {模块名称2} | {页面/功能描述2} | {用例数量2} | {高/中/低} | {备注2} |
| N | {模块名称N} | {页面/功能描述N} | {用例数量N} | {高/中/低} | {备注N} |
| N+1 | 全局检查 | 视觉一致性、交互反馈、可访问性、异常流程 | {全局用例数量} | 高 | 全局规范检查 |

**模块识别规则**：
- 根据需求文档的一级、二级功能模块识别
- 根据设计稿的页面结构识别
- 每个独立页面或功能模块作为一个走查模块
- 全局检查作为最后一个模块

**优先级定义**：
- **高**：核心功能页面，用户高频使用的页面
- **中**：次要功能页面，详情查看页面
- **低**：辅助功能页面，低频使用的页面

---

## 6. 模块串联逻辑与流程图

### 6.1 模块串联流程图

> **自动生成说明**：系统会根据需求文档和流程图，自动生成模块串联的Mermaid流程图。流程图应包含：
> - 入口页面和入口条件（如权限验证）
> - 主要页面跳转路径
> - 关键操作流程
> - 分支和决策点

```mermaid
graph TD
    A[{入口页面/系统}] --> B{入口条件验证}
    B -->|未通过| C[提示信息]
    B -->|通过| D[{主页面1}]
    
    D --> E[{操作1}]
    D --> F[{操作2}]
    
    E --> G[{页面2}]
    F --> H[{页面3}]
    
    G --> I[{页面4}]
    H --> J[{页面5}]
    
    style D fill:#e1f5ff
    style G fill:#fff4e6
    style I fill:#f0f9ff
```

**流程图生成规则**：
- 从需求文档中提取页面和操作
- 从流程图中提取跳转关系和决策点
- 识别主要成功路径和异常路径
- 标注关键页面和操作

---

## 7. 模块上下游影响分析

### 7.1 新增模块说明

本次新增的**{功能名称}**模块是{所属系统/平台}的一个{新功能/功能增强}模块，位于"{模块位置}"下。

> **自动生成说明**：系统会根据需求文档中的模块位置信息自动填充。

### 7.2 上游模块（依赖模块）

> **自动生成说明**：系统会根据需求文档中提到的依赖关系，自动识别上游模块。设计师需要补充填写影响分析。

| 上游模块 | 依赖关系 | 影响说明 | 验证要点 |
|----------|----------|----------|----------|
| {上游模块1} | {入口依赖/数据依赖/跳转依赖} | {影响说明1} | {验证要点1} |
| {上游模块2} | {依赖关系2} | {影响说明2} | {验证要点2} |
| {上游模块N} | {依赖关系N} | {影响说明N} | {验证要点N} |

**依赖关系类型**：
- **入口依赖**：新模块需要在其他模块的导航、菜单中显示
- **数据依赖**：新模块需要从其他模块获取数据（如下拉选择器、数据列表）
- **跳转依赖**：新模块需要跳转到其他模块（如帮助中心、资源管理）

**设计师填写**：
- 上游模块是否会影响{功能名称}模块的UI展示？【待填写】
- 上游模块的UI变更是否需要同步更新{功能名称}模块？【待填写】

### 7.3 下游模块（被影响模块）

> **自动生成说明**：系统会根据需求文档中提到的模块影响，自动识别下游模块。设计师需要补充填写影响分析。

| 下游模块 | 影响关系 | 影响说明 | 验证要点 |
|----------|----------|----------|----------|
| {下游模块1} | {新增Tab/复用逻辑/UI变更} | {影响说明1} | {验证要点1} |
| {下游模块2} | {影响关系2} | {影响说明2} | {验证要点2} |
| {下游模块N} | {影响关系N} | {影响说明N} | {验证要点N} |

**影响关系类型**：
- **新增Tab**：在其他模块页面中新增Tab或菜单项
- **复用逻辑**：复用其他模块的页面或组件
- **UI变更**：影响其他模块的UI显示

**设计师填写**：
- {功能名称}模块的UI变更是否会影响下游模块？【待填写】
- 是否需要在下游模块中增加{功能名称}相关的UI元素？【待填写】

### 7.4 上下游验证用例

基于上下游影响分析，需要在UI走查用例中增加以下验证点：

> **自动生成说明**：系统会根据上下游模块分析，自动生成验证用例编号和检查项。

1. **上游模块验证**：
   - UI-TC{编号1}：检查{上游模块1}中{功能名称}入口的显示和隐藏逻辑
   - UI-TC{编号2}：检查{上游模块2}的数据加载和空状态提示
   - UI-TC{编号N}：检查{上游模块N}的{验证内容N}

2. **下游模块验证**：
   - UI-TC{编号M}：检查{下游模块1}中{功能名称}相关UI元素的显示和功能
   - UI-TC{编号M+1}：检查{下游模块2}的UI一致性
   - UI-TC{编号M+N}：检查{下游模块N}的{验证内容N}

---

## 8. 补充内容

### 8.1 走查环境要求

| 环境类型 | 要求 | 说明 |
|----------|------|------|
| 浏览器 | Chrome 90+、Safari 14+、Firefox 88+ | 支持现代浏览器特性 |
| 屏幕尺寸 | 1920x1080、1366x768、1024x768 | 覆盖主流屏幕尺寸 |
| 网络环境 | 正常网络、弱网、断网 | 测试不同网络条件下的UI表现 |
| 权限环境 | {权限类型1}、{权限类型2} | 测试权限控制（根据实际需求填写） |

### 8.2 设计规范参考

设计规范参考地址：http://100.76.6.155:6611/components/dropdown.html

请在设计规范系统中查看以下内容：
- 颜色规范：主色、辅助色、错误色、成功色等
- 字体规范：字号、字重、行高等
- 间距规范：内边距、外边距、间距系统等
- 组件规范：按钮、输入框、下拉框、表格等组件的样式和交互规范

### 8.3 走查工具

| 工具类型 | 工具名称 | 用途 |
|----------|----------|------|
| 设计稿查看 | Pixso | 对比设计稿和实现效果 |
| 屏幕测量 | 浏览器开发者工具 | 测量尺寸、颜色、间距 |
| 截图工具 | Snipaste/系统截图 | 记录问题和对比效果 |
| 用例管理 | Excel/CSV | 记录和管理走查用例 |
| 问题跟踪 | 项目管理工具 | 跟踪问题修复进度 |

### 8.4 走查检查清单

#### 视觉一致性检查清单
- [ ] 颜色使用是否符合设计规范
- [ ] 字体类型、字号、字重是否统一
- [ ] 图标风格、尺寸、颜色是否一致
- [ ] 间距、边距是否符合设计规范
- [ ] 圆角、阴影等视觉效果是否一致

#### 组件状态检查清单
- [ ] 所有按钮的默认、悬停、点击、禁用、加载状态
- [ ] 所有输入框的占位符、已输入、聚焦、错误、禁用状态
- [ ] 所有下拉框的默认、展开、选中、禁用状态
- [ ] 所有链接的默认、已访问、悬停状态

#### 交互反馈检查清单
- [ ] 数据加载时是否有加载动画
- [ ] 操作成功/失败时是否有提示
- [ ] 页面跳转是否有过渡动画
- [ ] 按钮点击是否有反馈效果

#### 异常流程检查清单
- [ ] 输入验证错误提示是否清晰
- [ ] 操作失败时是否有错误提示和重试机制
- [ ] 空状态是否有友好提示
- [ ] 边界条件是否有相应处理
- [ ] 数据保护提示是否明确
- [ ] 删除操作是否有二次确认

#### 响应式检查清单
- [ ] 1920px宽度下的布局表现
- [ ] 1366px宽度下的布局表现
- [ ] 1024px宽度下的布局表现
- [ ] 内容过长时的截断处理

#### 可访问性检查清单
- [ ] 是否支持键盘导航
- [ ] 焦点指示是否清晰
- [ ] 颜色对比度是否满足WCAG标准
- [ ] 点击区域是否足够大

### 8.5 问题记录模板

| 问题编号 | 模块 | 检查点 | 问题描述 | 严重程度 | 优先级 | 负责人 | 状态 | 修复日期 |
|----------|------|--------|----------|----------|--------|--------|------|----------|
| UI-BUG-001 | {模块名称} | {检查点} | {问题描述} | {高/中/低} | {高/中/低} | 【待填写】 | 待修复 | 【待填写】 |

**严重程度**：高（阻塞功能）、中（影响体验）、低（轻微问题）

**优先级**：高（立即修复）、中（尽快修复）、低（后续修复）

---

## 9. 附录

### 9.1 相关文档

- 需求文档：`需求文档.md`
- UI走查规范：`ui_check_rules.md`
- UI走查用例：`UI用例/{功能名称}-UI走查用例-1.csv`
- 流程图：`流程图.md`

### 9.2 联系方式

如有疑问或需要补充信息，请联系：
- UI走查负责人：【待填写】
- 产品经理：【待填写】

---

**文档版本**：v1.0  
**创建日期**：{创建日期}  
**最后更新**：{最后更新日期}  
**文档状态**：待完善（需要填写团队信息和日期信息）

---

## 模板使用说明

### 占位符说明

本文档中的占位符需要根据实际需求文档进行替换：

- `{功能名称}`：替换为实际功能名称，如"跨域训练"、"用户登录"等
- `{模块数量}`：替换为实际模块数量
- `{用例数量}`：替换为实际用例数量
- `{模块名称N}`：替换为实际模块名称
- `{页面/功能描述N}`：替换为实际页面或功能描述
- `{所属系统/平台}`：替换为实际所属系统，如"智算平台"、"用户中心"等
- `{模块位置}`：替换为实际模块位置，如"高阶能力"、"基础功能"等

### 自动生成规则

当使用AI工具基于需求文档自动生成走查计划时，系统会：

1. **识别功能名称**：从需求文档标题或第一段中提取
2. **识别模块**：根据需求文档的章节结构识别功能模块
3. **识别流程**：从需求文档和流程图中提取核心业务流程
4. **识别上下游**：从需求文档中提取依赖关系和影响关系
5. **生成用例编号**：按照UI-TC001格式自动生成编号
6. **生成流程图**：根据模块关系和流程逻辑生成Mermaid流程图

### 手动填写内容

以下内容需要设计师或走查负责人手动填写：

- 走查团队信息（姓名、联系方式）
- 走查时间计划（开始日期、预计耗时）
- 上下游影响分析（设计师填写部分）
- 问题记录（走查过程中填写）

